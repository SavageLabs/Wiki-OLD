# Illyria's Wiki Repository

This wiki uses MkDocs, with sub folders for each plugin.

## run locally
Run `docker-compose up --build`
the app will then run on `localhost:8080`

## Deploy to production
`gcloud app deploy`